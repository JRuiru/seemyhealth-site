---
interface Props {
  name: string;
  image: string;
}

const { name, image } = Astro.props;
---

<section class="py-24 sm:py-32 bg-brand-black border-t border-white/5">
  <div class="max-w-5xl mx-auto px-6 lg:px-8 text-center">
    <p class="text-[11px] uppercase tracking-[4px] text-brand-gray-500 mb-4" data-scroll-reveal>See It in Action</p>
    <h2 class="font-display text-4xl sm:text-5xl font-300 uppercase leading-[0.95] mb-10" data-scroll-reveal>
      Watch the <span class="font-500">Video</span>
    </h2>
    <div class="relative aspect-video rounded-2xl overflow-hidden bg-brand-gray-900 border border-white/5" data-image-reveal>
      <div class="absolute inset-0 flex items-center justify-center z-10">
        <div class="w-20 h-20 rounded-full border-2 border-white/20 flex items-center justify-center hover:border-white/40 transition-colors cursor-pointer">
          <svg class="w-8 h-8 text-white ml-1" fill="currentColor" viewBox="0 0 24 24">
            <path d="M8 5v14l11-7z" />
          </svg>
        </div>
      </div>
      <img
        src={image}
        alt={`${name} video`}
        class="w-full h-full object-cover opacity-30"
        loading="lazy"
      />
    </div>
  </div>
</section>
